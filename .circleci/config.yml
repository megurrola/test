version: 2
jobs:
  build:
    docker:
      - image: hashicorp/packer
    working_directory: ~/packer
    steps:
#      - checkout
      - run:
          command: |
             wget -O test.txt --header='Accept: application/vnd.github.v3.raw' --header "Authorization: token $GITHUB_TOKEN" \
                  "https://api.github.com/repos/megurrola/test/contents/testbranch/dir/file.txt"
             ls -l
          name: Build image
#          command: packer build packer/jet8-code-nginx-phpfpm.json
